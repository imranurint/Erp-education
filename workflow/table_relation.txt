TABLE                   COLUMN              REFERENCES
─────                   ──────              ──────────

CORE:
users                   branch_id           → branches.branch_id
notifications           user_id             → users.user_id
audit_log               user_id             → users.user_id
user_sessions           user_id             → users.user_id

ACADEMICS:
fiscal_periods          year_id             → academic_years.year_id
academic_records        student_id          → students.student_id
academic_records        programme_id        → programmes.programme_id
academic_records        academic_year_id    → academic_years.year_id
academic_records        recorded_by         → users.user_id
academic_records        branch_id           → branches.branch_id
attendance              student_id          → students.student_id
attendance              programme_id        → programmes.programme_id
attendance              recorded_by         → users.user_id
attendance              branch_id           → branches.branch_id
programmes              branch_id           → branches.branch_id

STUDENTS:
students                programme_id        → programmes.programme_id
students                branch_id           → branches.branch_id
students                partner_id          → partners.partner_id
student_fee_setup       student_id          → students.student_id
student_fee_setup       programme_id        → programmes.programme_id
student_fee_setup       branch_id           → branches.branch_id
student_dues            student_id          → students.student_id
student_dues            coa_id              → chart_of_accounts.coa_id
student_dues            branch_id           → branches.branch_id
discounts               student_id          → students.student_id
discounts               approved_by         → users.user_id
discounts               branch_id           → branches.branch_id
student_documents       student_id          → students.student_id
student_documents       branch_id           → branches.branch_id
student_documents       uploaded_by         → users.user_id

PARTNERS:
partners                branch_id           → branches.branch_id

ACCOUNTING:
programme_fee_heads     programme_id        → programmes.programme_id
programme_fee_heads     coa_id              → chart_of_accounts.coa_id
payments                student_id          → students.student_id
payments                bank_coa_id         → chart_of_accounts.coa_id
payments                collected_by        → users.user_id
payments                approved_by         → users.user_id
payments                branch_id           → branches.branch_id
payments                fiscal_period_id    → fiscal_periods.period_id
payments                reversed_payment_id → payments.payment_id
payment_items           payment_id          → payments.payment_id
payment_items           coa_id              → chart_of_accounts.coa_id
payment_items           due_id              → student_dues.due_id
vouchers                prepared_by         → users.user_id
vouchers                approved_by         → users.user_id
vouchers                branch_id           → branches.branch_id
vouchers                fiscal_period_id    → fiscal_periods.period_id
voucher_entries         voucher_id          → vouchers.voucher_id
voucher_entries         coa_id              → chart_of_accounts.coa_id
voucher_entries         student_id          → students.student_id
voucher_entries         partner_id          → partners.partner_id
main_ledger             coa_id              → chart_of_accounts.coa_id
main_ledger             student_id          → students.student_id
main_ledger             voucher_id          → vouchers.voucher_id
main_ledger             voucher_entry_id    → voucher_entries.entry_id
main_ledger             fiscal_period_id    → fiscal_periods.period_id
main_ledger             branch_id           → branches.branch_id
student_ledger          student_id          → students.student_id
student_ledger          branch_id           → branches.branch_id
student_ledger          main_ledger_id      → main_ledger.ledger_id
petty_cash              approved_by         → users.user_id
petty_cash              branch_id           → branches.branch_id
petty_cash              created_by          → users.user_id
petty_cash              voucher_id          → vouchers.voucher_id
late_fee_charges        student_id          → students.student_id
late_fee_charges        due_id              → student_dues.due_id
late_fee_charges        branch_id           → branches.branch_id
late_fee_charges        voucher_id          → vouchers.voucher_id
late_fee_charges        approved_by         → users.user_id
late_fee_charges        waived_by           → users.user_id
financial_snapshots     branch_id           → branches.branch_id

PROGRESSION:
progression_records     student_id          → students.student_id
progression_records     university_id       → universities.university_id
progression_records     branch_id           → branches.branch_id
progression_records     created_by          → users.user_id
university_commissions  progression_record_id → progression_records.record_id
university_commissions  university_id       → universities.university_id
university_commissions  student_id          → students.student_id
university_commissions  branch_id           → branches.branch_id
university_commissions  created_by          → users.user_id
partner_commissions     partner_id          → partners.partner_id
partner_commissions     student_id          → students.student_id
partner_commissions     university_commission_id → university_commissions.commission_id
partner_commissions     branch_id           → branches.branch_id
partner_commissions     created_by          → users.user_id
partner_payments        partner_commission_id → partner_commissions.partner_commission_id
partner_payments        partner_id          → partners.partner_id
partner_payments        bank_coa_id         → chart_of_accounts.coa_id
partner_payments        branch_id           → branches.branch_id
partner_payments        created_by          → users.user_id
partner_payments        approved_by         → users.user_id

HRM (NEW):
employees               user_id             → users.user_id
employees               department_id       → departments.department_id
employees               designation_id      → designations.designation_id
employees               branch_id           → branches.branch_id
departments             head_user_id        → users.user_id
departments             branch_id           → branches.branch_id
designations            department_id       → departments.department_id
salary_structures       employee_id         → employees.employee_id
salary_structures       approved_by         → users.user_id
salary_structures       branch_id           → branches.branch_id
leave_requests          employee_id         → employees.employee_id
leave_requests          leave_type_id       → leave_types.leave_type_id
leave_requests          approved_by         → users.user_id
leave_requests          branch_id           → branches.branch_id
leave_balances          employee_id         → employees.employee_id
leave_balances          leave_type_id       → leave_types.leave_type_id
leave_balances          branch_id           → branches.branch_id
staff_attendance        employee_id         → employees.employee_id
staff_attendance        branch_id           → branches.branch_id
payroll                 voucher_id          → vouchers.voucher_id
payroll                 approved_by         → users.user_id
payroll                 branch_id           → branches.branch_id
payroll                 created_by          → users.user_id
payroll_items           payroll_id          → payroll.payroll_id
payroll_items           employee_id         → employees.employee_id
payroll_items           branch_id           → branches.branch_id
teacher_assignments     employee_id         → employees.employee_id
teacher_assignments     programme_id        → programmes.programme_id
teacher_assignments     academic_year_id    → academic_years.year_id
teacher_assignments     branch_id           → branches.branch_id
counselor_assignments   employee_id         → employees.employee_id
counselor_assignments   student_id          → students.student_id
counselor_assignments   branch_id           → branches.branch_id
student_meetings        student_id          → students.student_id
student_meetings        employee_id         → employees.employee_id
student_meetings        branch_id           → branches.branch_id

NOTIFICATIONS (NEW):
generated_documents     generated_by        → users.user_id
generated_documents     branch_id           → branches.branch_id
email_queue             branch_id           → branches.branch_id

TOTAL FOREIGN KEYS: 92 relationships across 47 tables